name: Publish Site
run-name: Publish site / ${{ inputs.requestId }}

on:
  workflow_dispatch:
    inputs:
      requestId:
        description: Internal request id for manager status polling
        required: true
        type: string

permissions:
  contents: read

concurrency:
  group: site-publish-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy-cloudflare-worker:
    name: Build and deploy Cloudflare Worker
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - name: Check out the selected commit
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Verify generated Cloudflare bindings
        run: npm run types:cloudflare -- --check

      - name: Build the Astro site
        run: npm run build

      - name: Deploy the Worker and static assets
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID || vars.CLOUDFLARE_ACCOUNT_ID }}
        run: node scripts/run-wrangler.mjs deploy
