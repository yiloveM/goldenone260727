---
import IndustryHome from '../components/IndustryHome.astro';
import type { NonEnglishLocale } from './localizedPageHelpers';

interface Props {
  locale: NonEnglishLocale;
}

const { locale } = Astro.props;
---

<IndustryHome locale={locale} />
